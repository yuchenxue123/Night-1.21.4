package cute.neko.night.ui.screen.click.styles.drop

/**
 * @author yuchenxue
 * @date 2025/08/08
 */

object Constants {

    // 抽屉的圆角
    const val CATEGORY_RADIUS = 8f

    // 抽屉的宽度
    const val COMPONENT_WIDTH = 240f

    // 抽屉上面那个条的高度
    const val COMPONENT_HEIGHT = 50f

    // 抽屉上面那个条和下面那些模块之间的距离
    const val CATEGORY_BUTTON_SPACE = 4f

    // 抽屉底下有一点空间，因为剪刀是矩形的
    // 但是这个剪刀根本没什么用，因为没有上下滚轮滑动
    const val CATEGORY_BOTTOM_SPACE = CATEGORY_RADIUS

    // 模块名左边距离
    const val MODULE_TEXT_LEFT_SPACE = 16f

    // 同上，但是模块设置选项
    const val SETTING_LEFT_RIGHT_SPACE = MODULE_TEXT_LEFT_SPACE

    // 模块选项的初始高度
    const val SETTING_BUTTON_HEIGHT = 40f

    // 布兰兹选项按钮的宽度
    const val TOGGLE_BUTTON_WIDTH = 50f

    const val SELECT_BUTTON_HEIGHT = 30f

    const val MODULE_ARROW_SIZE = 8f

    const val SELECT_SETTING_ARROW_SIZE = 6f
}